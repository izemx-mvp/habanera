import { Download, Printer } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { downloadPdf, printPdf, type OfficialDocument } from "@/lib/pdf";

export function DocumentPreview({ document, open, onOpenChange }: { document: OfficialDocument | null; open: boolean; onOpenChange: (open: boolean) => void }) {
  if (!document) return null;
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-h-[92vh] max-w-3xl overflow-y-auto"><DialogHeader><DialogTitle>{document.kind}</DialogTitle><DialogDescription>{document.reference} · Aperçu avant impression</DialogDescription></DialogHeader><div className="border bg-card p-6 shadow-sm"><div className="border-b-2 border-primary pb-4"><p className="font-display text-2xl text-primary">HABANERA</p><p className="text-xs text-muted-foreground">MEAT · WINE · SHOW — ÉCONOMAT MARRAKECH</p></div><div className="grid gap-2 py-5 text-sm sm:grid-cols-2">{document.metadata.map(([label,value])=><p key={label}><span className="font-medium">{label} :</span> {value}</p>)}</div><div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="bg-primary text-primary-foreground"><tr>{document.columns.map((column)=><th key={column} className="px-3 py-2">{column}</th>)}</tr></thead><tbody>{document.rows.map((row,index)=><tr key={`${document.reference}-${index}`} className="border-b">{row.map((cell,cellIndex)=><td key={cellIndex} className="px-3 py-2">{cell}</td>)}</tr>)}</tbody></table></div>{document.total&&<p className="mt-4 text-right font-semibold">{document.total}</p>}<div className="mt-10 grid grid-cols-3 gap-6 text-center text-xs"><span className="border-t pt-2">Établi par</span><span className="border-t pt-2">Contrôle économat</span><span className="border-t pt-2">Signature / cachet</span></div></div><DialogFooter><Button variant="outline" onClick={()=>void printPdf(document)}><Printer className="mr-2 h-4 w-4"/>Imprimer</Button><Button onClick={()=>void downloadPdf(document)}><Download className="mr-2 h-4 w-4"/>Télécharger le PDF</Button></DialogFooter></DialogContent></Dialog>;
}