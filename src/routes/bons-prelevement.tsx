import { createFileRoute } from "@tanstack/react-router";
import { CheckCircle2, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";
import { AppShell } from "@/components/app-shell"; import { DocumentPreview } from "@/components/document-preview";
import { Badge } from "@/components/ui/badge"; import { Button } from "@/components/ui/button"; import { Card, CardContent } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select"; import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { useOperations } from "@/lib/operations"; import type { OfficialDocument } from "@/lib/pdf";
export const Route=createFileRoute("/bons-prelevement")({head:()=>({meta:[{title:"Bons de prélèvement — Habanera"},{name:"description",content:"Validation et traçabilité des prélèvements Habanera."},{property:"og:title",content:"Bons de prélèvement — Habanera"},{property:"og:description",content:"Préparez les sorties de stock."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});
function Page(){const{articles,validateWithdrawal}=useOperations();const[search,setSearch]=useState("");const[destination,setDestination]=useState("Tous");const[level,setLevel]=useState("Tous");const[done,setDone]=useState<string[]>([]);const[quantities,setQuantities]=useState<Record<string,number>>({});const[document,setDocument]=useState<OfficialDocument|null>(null);const eligible=useMemo(()=>articles.filter(a=>a.point!=="Économat"&&a.ventes>0&&a.nom.toLowerCase().includes(search.toLowerCase())&&(destination==="Tous"||a.point===destination)&&(level==="Tous"||(level==="Critique"?a.stock<a.seuil:a.stock>=a.seuil))),[articles,search,destination,level]);function validate(id:string){const a=articles.find(x=>x.id===id);if(!a)return;const quantity=quantities[id]??Math.min(a.stock,a.ventes);validateWithdrawal(id,quantity);setDone(d=>[...d,id]);setDocument({kind:"BON DE PRÉLÈVEMENT",reference:`BP-${Date.now().toString().slice(-6)}`,date:new Date().toLocaleString("fr-FR"),subtitle:"Sortie officielle de l’économat",metadata:[["Destination",a.point],["Validé par","Salah Bennani — Administrateur"]],columns:["Produit","Unité","Stock avant","Quantité prélevée","Destination"],rows:[[a.nom,a.unite,a.stock,quantity,a.point]],note:"La signature confirme la remise physique des quantités au service destinataire."});toast.success("Prélèvement validé et bon généré.")}return <AppShell title="Bons de Prélèvement" subtitle={`${eligible.filter(a=>!done.includes(a.id)).length} suggestions à valider`}><Card className="mb-4"><CardContent className="grid gap-3 p-4 md:grid-cols-[1fr_180px_180px]"><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input className="pl-9" placeholder="Rechercher un produit" value={search} onChange={e=>setSearch(e.target.value)}/></div><Select value={destination} onValueChange={setDestination}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{["Tous","Bar","Cuisine","Pâtisserie"].map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select><Select value={level} onValueChange={setLevel}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="Tous">Tous les niveaux</SelectItem><SelectItem value="Critique">Sous le seuil</SelectItem><SelectItem value="Normal">Stock normal</SelectItem></SelectContent></Select></CardContent></Card><Card><CardContent className="p-5"><div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Produit</TableHead><TableHead>Destination</TableHead><TableHead>Stock fixe</TableHead><TableHead>Ventes</TableHead><TableHead>Suggestion</TableHead><TableHead className="text-right">Action</TableHead></TableRow></TableHeader><TableBody>{eligible.map(a=><TableRow key={a.id}><TableCell className="font-medium">{a.nom}</TableCell><TableCell>{a.point}</TableCell><TableCell>{a.stockInitial} {a.unite}</TableCell><TableCell>{a.ventes} {a.unite}</TableCell><TableCell><Input className="w-24" type="number" min={1} max={a.stock} value={quantities[a.id]??Math.min(a.stock,a.ventes)} onChange={e=>setQuantities(q=>({...q,[a.id]:Number(e.target.value)}))}/></TableCell><TableCell className="text-right">{done.includes(a.id)?<Badge variant="success"><CheckCircle2 className="mr-1 h-3 w-3"/>Validé</Badge>:<Button size="sm" onClick={()=>validate(a.id)}>Valider le prélèvement</Button>}</TableCell></TableRow>)}</TableBody></Table></div></CardContent></Card><DocumentPreview document={document} open={Boolean(document)} onOpenChange={o=>{if(!o)setDocument(null)}}/></AppShell>}