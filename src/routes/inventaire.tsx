import{createFileRoute}from"@tanstack/react-router";import{Archive,Search}from"lucide-react";import{useMemo,useState}from"react";import{toast}from"sonner";import{AppShell}from"@/components/app-shell";import{DocumentPreview}from"@/components/document-preview";import{Badge}from"@/components/ui/badge";import{Button}from"@/components/ui/button";import{Card,CardContent}from"@/components/ui/card";import{Input}from"@/components/ui/input";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{Table,TableBody,TableCell,TableHead,TableHeader,TableRow}from"@/components/ui/table";import{formatMAD}from"@/lib/habanera-data";import{useOperations,type InventoryReport}from"@/lib/operations";import type{OfficialDocument}from"@/lib/pdf";
export const Route=createFileRoute("/inventaire")({head:()=>({meta:[{title:"Inventaire — Habanera"},{name:"description",content:"Comparatif des stocks et démarque."},{property:"og:title",content:"Inventaire — Habanera"},{property:"og:description",content:"Clôture et rapport d’inventaire."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});
function Page(){const{articles,updateArticle,inventoryReports,closeInventory}=useOperations();const[counts,setCounts]=useState<Record<string,number>>(()=>Object.fromEntries(articles.map(a=>[a.id,a.stock])));const[search,setSearch]=useState("");const[point,setPoint]=useState("Tous");const[critical,setCritical]=useState("Tous");const[document,setDocument]=useState<OfficialDocument|null>(null);const rows=useMemo(()=>articles.filter(a=>a.nom.toLowerCase().includes(search.toLowerCase())&&(point==="Tous"||a.point===point)&&(critical==="Tous"||Math.abs((counts[a.id]??a.stock)-a.stock)>=3)),[articles,search,point,critical,counts]);function reportDoc(report:InventoryReport){setDocument({kind:"RAPPORT D’INVENTAIRE",reference:report.id,date:report.date,subtitle:"Clôture officielle de période",metadata:[["Responsable",report.author],["Articles comptés",String(report.lines.length)]],columns:["Produit","Théorique","Réel","Écart","Valeur"],rows:report.lines.map(l=>[l.article,`${l.theoretical} ${l.unit}`,`${l.real} ${l.unit}`,l.gap,formatMAD(l.value)]),total:`Impact financier : ${formatMAD(report.totalGap)}`,note:"Les écarts constatés sont transmis au suivi des anomalies."})}function close(){const lines=articles.map(a=>{const real=counts[a.id]??a.stock;return{article:a.nom,theoretical:a.stock,real,gap:real-a.stock,value:(real-a.stock)*a.prixAchat,unit:a.unite}});const report=closeInventory({date:new Date().toLocaleString("fr-FR"),author:"Salah Bennani",totalGap:lines.reduce((s,l)=>s+l.value,0),lines});reportDoc(report);toast.success("Inventaire clôturé et rapport généré.")}return <AppShell title="Inventaire" subtitle="Comparatif théorique, réel et démarque" action={<Button onClick={close}><Archive className="mr-2 h-4 w-4"/>Clôturer l’inventaire</Button>}><Card className="mb-4"><CardContent className="grid gap-3 p-4 md:grid-cols-[1fr_180px_190px]"><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input className="pl-9" placeholder="Rechercher un produit" value={search} onChange={e=>setSearch(e.target.value)}/></div><Select value={point} onValueChange={setPoint}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{["Tous","Bar","Cuisine","Économat"].map(v=><SelectItem key={v} value={v}>{v}</SelectItem>)}</SelectContent></Select><Select value={critical} onValueChange={setCritical}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="Tous">Tous les écarts</SelectItem><SelectItem value="Critiques">Écarts critiques</SelectItem></SelectContent></Select></CardContent></Card><Card><CardContent className="overflow-x-auto p-5"><Table><TableHeader><TableRow><TableHead>Produit</TableHead><TableHead>Théorique</TableHead><TableHead>Stock réel</TableHead><TableHead>Écart</TableHead><TableHead>Valeur écart</TableHead><TableHead className="text-right">Action</TableHead></TableRow></TableHeader><TableBody>{rows.map(a=>{const real=counts[a.id]??a.stock,gap=real-a.stock;return<TableRow key={a.id}><TableCell className="font-medium">{a.nom}<p className="text-xs text-muted-foreground">{a.point}</p></TableCell><TableCell>{a.stock} {a.unite}</TableCell><TableCell><Input className="w-28" type="number" min={0} value={real} onChange={e=>setCounts(c=>({...c,[a.id]:Number(e.target.value)}))}/></TableCell><TableCell><Badge variant={gap===0?"success":Math.abs(gap)>=3?"destructive":"warning"}>{gap>0?`+${gap}`:gap} {a.unite}</Badge></TableCell><TableCell className={gap<0?"text-destructive":"text-success"}>{formatMAD(gap*a.prixAchat)}</TableCell><TableCell className="text-right"><Button size="sm" variant="outline" disabled={gap===0} onClick={()=>{updateArticle(a.id,{stock:real});toast.success(`Stock de ${a.nom} ajusté.`)}}>Ajuster le stock</Button></TableCell></TableRow>})}</TableBody></Table></CardContent></Card>{inventoryReports.length>0&&<section className="mt-6"><h2 className="mb-3 text-lg">Historique des clôtures</h2><div className="grid gap-3 md:grid-cols-3">{inventoryReports.map(r=><button key={r.id} onClick={()=>reportDoc(r)} className="rounded-lg border bg-card p-4 text-left transition-colors hover:border-primary"><p className="font-medium">{r.id}</p><p className="text-sm text-muted-foreground">{r.date}</p><p className="mt-2 text-sm">Impact : {formatMAD(r.totalGap)}</p></button>)}</div></section>}<DocumentPreview document={document} open={Boolean(document)} onOpenChange={o=>{if(!o)setDocument(null)}}/></AppShell>}